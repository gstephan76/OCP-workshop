apiVersion: v1
kind: Pod
metadata:
  name: nginx-2
  namespace: grp1-common
spec:
  containers:
    - name: nginx-2
      image: nginx
      resources:
        limits:
          memory: "900Mi"
          cpu: "500m"
